import { SxProps } from "@mui/material";
const styles: Record<string, SxProps> = {
  footer: {
    width: "100%",
    color:"#fff",
    fontSize:"12px",
    fontFamily:"poppins",
    textAlign:"center",
    padding: "20px 0px",
    background: "#000000",
  },
};

export default styles;
